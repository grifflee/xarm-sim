# other
*.ply
*.exr

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local and generated simulation data
tmpgenesis/
__nyx_cache__/
outputs/
logs/
last.ply
*.mp4
left.png
depth.png
depth.npy

# Machine-specific runtime env (CUDA_HOME + the libnvJitLink LD_PRELOAD that gs_madrona
# needs). Per-host, so it is not tracked -- the trap it works around is documented in
# AGENTS.md under "RUNTIME TRAP", which is where a new machine should start.
luc_env.sh
